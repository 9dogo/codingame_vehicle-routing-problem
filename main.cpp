#include <Client.h>
#include <Utils.h>
#include <Vehicle.h>

#include <iostream>
#include <vector>

using std::vector;

int main() {
    vector<Client> clients;
    vector<Vehicle> vehicles;

    // to implement : TestReader.readTest(clients, vehicles) (or Utils.readTest(clients, vehicles))

    std::cout << "optimal vehicle paths" << std::endl;
}